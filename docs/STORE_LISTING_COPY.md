# TokenTint Chrome Web Store listing copy

Use one matching listing per Chrome Web Store locale. The extension package reads the localized name and summary from `_locales`; paste the detailed description into the Developer Dashboard. Keep the same feature scope across locales.

## English (`en`)

**Name**

TokenTint – Color Picker & Design Tokens

**Short description (107 characters)**

Pick colors from webpages, save palettes, copy HEX/RGB/HSL, and export CSS, Tailwind, or W3C design tokens.

**Detailed description**

TokenTint is a color picker and design token tool for Chrome. Capture a color from the current webpage with the EyeDropper API, copy HEX, RGB, or HSL values, and keep a local color history for quick reuse.

Features:

- Pick colors from the screen with the browser color picker.
- Save colors to a project palette and manage your design colors.
- Copy HEX, RGB, and HSL values.
- Export CSS Variables in the free version.
- Pro adds multiple project palettes, Tailwind Config export, W3C Design Tokens export, and optional extraction of key computed colors from the active webpage.
- Use light or dark theme settings.

Color history, palettes, settings, and local entitlement state are stored in Chrome local storage. The TokenTint extension has no advertising or product usage analytics. The website uses limited analytics separately. Page colors are read only after you choose Extract Page Colors, and page content is not uploaded to TokenTint servers.

Some Pro features require a one-time purchase. Payment is handled by Creem on the TokenTint website. See the privacy policy, terms, and refund policy before purchasing.

## 简体中文 (`zh_CN`)

**名称**

TokenTint – 取色器与设计令牌

**简介**

从网页取色、保存调色板、复制 HEX/RGB/HSL，并导出 CSS、Tailwind 或 W3C 设计令牌。

**详情说明**

TokenTint 是一款适用于 Chrome 的取色器和设计令牌工具。使用浏览器取色器从当前网页获取颜色，复制 HEX、RGB 或 HSL 数值，并保存本地颜色历史记录。

主要功能：

- 从网页屏幕快速取色。
- 将颜色保存到项目调色板，整理设计颜色。
- 复制 HEX、RGB、HSL 色值。
- 免费版支持导出 CSS 变量。
- Pro 支持多个项目调色板、Tailwind Config 导出、W3C Design Tokens 导出，以及按需提取当前网页的关键计算样式颜色。
- 支持浅色和深色主题。

颜色历史、调色板、设置和本地授权状态保存在 Chrome 本地存储中。TokenTint 不包含广告或分析功能。只有在你主动点击“提取页面颜色”后，扩展才会读取当前页面的计算样式颜色，页面内容不会上传到 TokenTint 服务器。

部分 Pro 功能需要一次性购买，支付由 TokenTint 网站上的 Creem 处理。购买前请查看隐私政策、条款和退款政策。

## Deutsch (`de`)

**Name**

TokenTint – Farbwähler und Design-Tokens

**Kurzbeschreibung**

Farben aus Webseiten aufnehmen, Paletten speichern, HEX/RGB/HSL kopieren und CSS-, Tailwind- oder W3C-Design-Tokens exportieren.

**Detaillierte Beschreibung**

TokenTint ist ein Farbwähler und Design-Token-Tool für Chrome. Erfassen Sie Farben aus Webseiten mit dem Browser-Farbwähler, kopieren Sie HEX-, RGB- oder HSL-Werte und speichern Sie eine lokale Farbchronik.

- Farben direkt vom Bildschirm aufnehmen.
- Farben in Projektpaletten speichern und organisieren.
- HEX-, RGB- und HSL-Werte kopieren.
- CSS-Variablen exportieren.
- Pro bietet mehrere Projektpaletten, Tailwind Config, W3C Design Tokens und die optionale Extraktion wichtiger berechneter Farben der aktiven Webseite.
- Helles oder dunkles Design verwenden.

Farbchronik, Paletten, Einstellungen und lokaler Lizenzstatus bleiben im Chrome-Speicher. Es gibt keine Werbung oder Analyse. Webseitenfarben werden nur nach dem Klick auf „Seitenfarben extrahieren“ gelesen und nicht an TokenTint übertragen.

## Español (`es`)

**Nombre**

TokenTint – Selector de color y tokens de diseño

**Descripción detallada**

TokenTint es un selector de color y una herramienta de tokens de diseño para Chrome. Captura colores de páginas web, copia valores HEX, RGB o HSL y guarda un historial local para reutilizarlo rápidamente.

- Extrae colores de la pantalla con el selector del navegador.
- Guarda colores en paletas de proyectos.
- Copia valores HEX, RGB y HSL.
- Exporta variables CSS.
- Pro añade varias paletas, exportación de Tailwind Config, W3C Design Tokens y extracción opcional de colores calculados de la página activa.
- Elige entre tema claro y oscuro.

El historial, las paletas, la configuración y el estado de licencia se guardan localmente en Chrome. No hay publicidad ni analítica. La página activa solo se lee después de pulsar “Extraer colores de la página” y su contenido no se envía a los servidores de TokenTint.

## Français (`fr`)

**Nom**

TokenTint – Pipette de couleurs et design tokens

**Description détaillée**

TokenTint est une pipette de couleurs et un outil de design tokens pour Chrome. Prélevez des couleurs sur les pages web, copiez les valeurs HEX, RGB ou HSL et conservez un historique local.

- Prélevez une couleur à l’écran avec la pipette du navigateur.
- Enregistrez les couleurs dans des palettes de projets.
- Copiez les valeurs HEX, RGB et HSL.
- Exportez des variables CSS.
- Pro ajoute plusieurs palettes, l’export Tailwind Config, l’export W3C Design Tokens et l’extraction optionnelle des couleurs calculées importantes de la page active.
- Choisissez un thème clair ou sombre.

L’historique, les palettes, les réglages et l’état de licence sont stockés localement dans Chrome. TokenTint n’utilise ni publicité ni analyse. La page active est lue uniquement après votre clic sur « Extraire les couleurs de la page » et son contenu n’est pas envoyé aux serveurs TokenTint.

## 日本語 (`ja`)

**名前**

TokenTint – カラーピッカーとデザイントークン

**詳細説明**

TokenTintは、Chromeで使えるカラーピッカーとデザイントークン管理ツールです。ウェブページから色を取得し、HEX・RGB・HSLの値をコピーして、ローカルのカラー履歴に保存できます。

- ブラウザのカラーピッカーで画面から色を取得。
- 色をプロジェクトパレットに保存して整理。
- HEX・RGB・HSLの値をコピー。
- CSS変数を書き出し。
- Proでは複数のパレット、Tailwind Config、W3C Design Tokensの書き出し、アクティブページの主要な計算済みカラーの任意抽出に対応。
- ライトテーマとダークテーマを選択。

カラー履歴、パレット、設定、ライセンス状態はChromeのローカルストレージに保存されます。広告や分析はありません。ページの色は「ページの色を抽出」をクリックした場合のみ読み取られ、ページ内容はTokenTintのサーバーに送信されません。

## 한국어 (`ko`)

**이름**

TokenTint – 색상 추출기와 디자인 토큰

**상세 설명**

TokenTint는 Chrome용 색상 추출기이자 디자인 토큰 도구입니다. 웹페이지에서 색상을 추출하고 HEX, RGB, HSL 값을 복사하여 로컬 색상 기록에 저장할 수 있습니다.

- 브라우저 색상 추출기로 화면에서 색상 추출.
- 색상을 프로젝트 팔레트에 저장하고 정리.
- HEX, RGB, HSL 값 복사.
- CSS 변수 내보내기.
- Pro에서는 여러 프로젝트 팔레트, Tailwind Config, W3C Design Tokens 내보내기와 활성 페이지의 주요 계산 색상 선택 추출을 지원합니다.
- 라이트 테마와 다크 테마 지원.

색상 기록, 팔레트, 설정 및 라이선스 상태는 Chrome 로컬 저장소에 저장됩니다. 광고와 분석 기능은 없습니다. 페이지 색상은 사용자가 “페이지 색상 추출”을 클릭한 경우에만 읽으며, 페이지 내용은 TokenTint 서버로 전송되지 않습니다.

## ASO keyword map

Use these naturally in title, summary, description, and screenshot captions where they fit the locale: color picker, eyedropper, color sampler, palette, HEX, RGB, HSL, CSS Variables, design tokens, Tailwind Config, W3C Design Tokens, webpage color extraction, 取色器, 调色板, 设计令牌.

Do not create a separate keyword list in the description or repeat one phrase unnaturally. Chrome Web Store metadata must remain accurate, descriptive, and consistent with the installed product.

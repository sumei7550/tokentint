import type { Metadata } from 'next';
import SeoLandingPage from '@/components/SeoLandingPage';
export const metadata: Metadata = { title: 'Color Picker Chrome Extension', description: 'Pick colors from any website with TokenTint, save a useful history, and turn selected colors into CSS and design tokens.', alternates: { canonical: '/color-picker-chrome-extension' } };
export default function Page() { return <SeoLandingPage h1="Color Picker Chrome Extension for Developers" intro="A practical Chrome extension for sampling interface colors and carrying them into the code and design-system work that follows." problem="A basic eyedropper gives you one color, but product work rarely ends there. You still need to remember the source, compare related colors, and convert the result into formats your project can use." solution="TokenTint keeps a 20-color history, copies HEX, RGB, and HSL values, and gives you a path from a sampled pixel to a project palette and token export." workflow={['Open TokenTint while viewing a webpage and activate the EyeDropper.', 'Sample the colors that matter, then copy the format your current task needs.', 'Organize the picks into a palette and export CSS variables, Tailwind config, or W3C tokens when the project is ready.']} free={['EyeDropper color picking', 'HEX, RGB, and HSL formats', '20-color history', 'Single project palette', 'CSS Variables export']} pro={['Everything in Free', 'Page color extraction', 'Multiple project palettes', 'Tailwind config export', 'W3C Design Tokens export']} why="it is built around the handoff from visual reference to reusable project values, so you spend less time transcribing colors and more time shipping consistent UI." related={[{href:'/website-color-picker',label:'Pick colors from a website'},{href:'/design-token-generator',label:'Generate design tokens'},{href:'/tailwind-color-generator',label:'Create Tailwind colors'}]} faqs={[{question:'Is TokenTint free?',answer:'Yes. The core picker, color formats, history, one project palette, and CSS Variables export are free. Pro is a one-time upgrade for advanced workflows.'},{question:'Does TokenTint work on any website?',answer:'You can use the picker on webpages when you actively invoke it through the extension and the page supports the browser color sampling workflow.'},{question:'How is it different from a basic eyedropper?',answer:'TokenTint connects sampling with history, palettes, and developer-friendly exports instead of stopping at a single copied color.'}]} />; }
